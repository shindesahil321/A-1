# Frontend API base URL for production/staging deployments
# Leave empty for local development (uses proxy from package.json)
# Example for production: https://your-backend-url.com
REACT_APP_API_BASE=
