# MongoDB connection string - get from MongoDB Atlas
MONGO_CONN=mongodb+srv://username:password@cluster.mongodb.net/?appName=AppName

# JWT secret for token signing - use a strong random string
JWT_SECRET=your_jwt_secret_key_here

# Optional: Set port for local development (Render/Vercel will override this)
PORT=8080
